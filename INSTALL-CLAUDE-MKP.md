<!-- Part of MKP Claude integration pack v1.0.2 – update here, then re-export the pack. -->
# INSTALL-CLAUDE-MKP (Pack v1.0.2)

1) Copy `.claude/` to repo root.
2) Merge `CLAUDE.md` (append if exists).
3) Paste `docs/README-CLAUDE-snippet.md` into your main README.

After install: run `/mkp-audit`; use `/mkp-task`; delegate heavy questions to `mkp-analyst`.
